__pycache__/
*.pyc
.venv/
venv/
.DS_Store
*.ipynb_checkpoints

# v2 checker reply cache
evals/cache/

# gate predictions (run_gate.py --preds input files)
evals/preds/

# future model-build outputs (tokenizer artifacts, tokenized corpus, checkpoints)
model/tokenizer/
model/data/
model/checkpoints/

# the eval sets in evals/data/ must stay tracked (frozen set is hash-pinned
# in docs/DESIGN.md) -- nothing above matches them; keep it that way.
